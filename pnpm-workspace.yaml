packages:
  - 'apps/*'

# Scripts de build permitidos (pnpm 11 los bloquea por defecto).
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  prisma: true
  esbuild: true
  unrs-resolver: true
  # Telemetría de terceros, no hace falta.
  '@scarf/scarf': false
